{
  "name": "pothole-reporter",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:api": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy . --project-name=fixstreet"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
